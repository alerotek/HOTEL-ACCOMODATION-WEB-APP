# Reports

This folder will contain reporting components and utilities (charts, PDF generation, analytics helpers).

Placeholders only — implement charts (e.g., using Chart.js or Recharts) and PDF export (jsPDF) here.
