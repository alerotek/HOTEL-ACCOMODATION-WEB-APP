// supabaseClient.ts
// Placeholder Supabase client export. Replace with actual client creation
// using `createClient` from `@supabase/supabase-js` and environment variables.

// Example:
// import { createClient } from '@supabase/supabase-js';
// export const supabase = createClient(
//   process.env.NEXT_PUBLIC_SUPABASE_URL!,
//   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
// );

// NOTE: Keep service role keys and other secrets out of the frontend. Use Edge
// Functions or server-side routes for privileged operations.

export const supabase: any = null; // TODO: initialize client when ready
